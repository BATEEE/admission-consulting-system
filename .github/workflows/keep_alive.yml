name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping app
        run: |
          curl --silent --show-error --fail \
          https://admission-consulting-system.onrender.com/career-service/health
